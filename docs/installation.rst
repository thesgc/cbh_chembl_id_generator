============
Installation
============

At the command line::

    $ easy_install cbh_chembl_id_generator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cbh_chembl_id_generator
    $ pip install cbh_chembl_id_generator
